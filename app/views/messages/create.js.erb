<% if @message.errors.any? %>
<% else %>
  const messagesContainer = document.querySelector('.message');
   message.Container.insertAdjacentHTML('beforeend', '<%= j render "message", message: user_is_messages_author: true)')
  const input = document.querySelector('#message_content');
  input.value = '';
  input.blur();
  scrollLastMessageIntoView();
 <% end %>
